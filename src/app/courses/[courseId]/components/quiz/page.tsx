// src/app/courses/[courseId]/components/quiz/page.tsx
"use client";

// Remove these because they’re never used:
// import type { Question, Option } from "@/lib/types";

// Also fix or remove the incorrect QuizPageClient import.
// If you don’t actually have a QuizPageClient.tsx next to this file,
// simply delete the import altogether:

// import QuizPageClient from "./QuizPageClient";

export default function QuizPlaceholder() {
  // You can return a stub (or a redirect), or just remove this file if it isn’t needed.
  return (
    <div className="p-6 text-center text-gray-600">
      {/* This component is no longer in use. */}
      Quiz “page.tsx” under components/quiz has been removed or relocated.
    </div>
  );
}
